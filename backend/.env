# --- Server Configuration ---
# The port your Express server will run on. 5000 is a common choice for development.
PORT=5000

# --- MongoDB Configuration ---
# Your MongoDB connection string. Replace with your actual database connection details.
# For local MongoDB: MONGO_URI=mongodb://localhost:27017/your_database_name
# For MongoDB Atlas: 
# Using the Expanded String because your network cannot resolve SRV records (querySrv ECONNREFUSED)
MONGO_URI=mongodb://rkt2classes_db_user:ROyae8c9TRJS2Kol@ac-mtk8uxi-shard-00-00.uwxnetw.mongodb.net:27017,ac-mtk8uxi-shard-00-01.uwxnetw.mongodb.net:27017,ac-mtk8uxi-shard-00-02.uwxnetw.mongodb.net:27017/datacoin?ssl=true&replicaSet=atlas-w0eush-shard-0&authSource=admin&retryWrites=true&w=majority
# If your environment blocks SRV/DNS lookups you can replace the above with
# the "Standard connection string" from Atlas (non-SRV). Example template:
# mongodb://<user>:<password>@cluster0-shard-00-00.uwxnetw.mongodb.net:27017,cluster0-shard-00-01.uwxnetw.mongodb.net:27017,cluster0-shard-00-02.uwxnetw.mongodb.net:27017/datacoin?ssl=true&replicaSet=<replicaSetName>&authSource=admin&retryWrites=true&w=majority
# --- JSON Web Token (JWT) Configuration ---
# A long, complex, and random secret string used to sign and verify your JWTs.
# Keep this value private and secure. You can generate one using an online tool.
JWT_SECRET=your_super_secret_and_long_random_string_for_jwt

# --- CORS Configuration ---
# The URL of your frontend React application. This tells the server to accept
# requests from this origin. You can provide a comma-separated list when you
# need to permit multiple hosts (e.g. local dev + Netlify):
#
#   CLIENT_URL=http://localhost:3000,https://tubular-begonia-3a766f.netlify.app
#
# The code will split the string and automatically whitelist each value.
CLIENT_URL=http://localhost:3000